services:
  simplebrain:
    build: .
    env_file: .env
    ports:
      - "8000:8000"
    volumes:
      - brain-data:/data/brain
    extra_hosts:
      - "host.docker.internal:host-gateway"
    restart: unless-stopped

volumes:
  brain-data:
